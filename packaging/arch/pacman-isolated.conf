# Test-only configuration for an unprivileged, networkless temporary root.
# Never use this configuration to install a downloaded or public package.
[options]
Architecture = auto
SigLevel = Never
LocalFileSigLevel = Never
DownloadUser = root
ParallelDownloads = 1
